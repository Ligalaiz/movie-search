import { navbarComponents } from '../components';

const result = /* html */ `
  <div class="container detail__container">
    ${navbarComponents}
    <div class="detail__movie" id="detailMovie"></div>
  </div>
`;

export default result;
