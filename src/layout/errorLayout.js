import { navbarComponents, errorComponents } from '../components';

const main = /* html */ `
  <div class="container">
    ${navbarComponents}
    ${errorComponents}
  </div>
`;

export default main;
