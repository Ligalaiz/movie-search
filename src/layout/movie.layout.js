import { navbarComponents, movieComponents } from '../components';

const movie = /* html */ `
  <div class="container search__container">
    ${navbarComponents}
    ${movieComponents}
  </div>
`;

export default movie;
